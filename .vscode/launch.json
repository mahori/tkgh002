{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "スクリプトの実行: dev",
            "request": "launch",
            "command": "npm run dev",
            "cwd": "${workspaceFolder}"
        }
    ]
}
